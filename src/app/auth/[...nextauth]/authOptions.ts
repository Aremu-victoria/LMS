// Re-export shared authOptions from the lib folder to avoid importing API route modules
export { authOptions } from '../../../lib/authOptions';

// also provide a default export for convenience
export { default } from '../../../lib/authOptions';
